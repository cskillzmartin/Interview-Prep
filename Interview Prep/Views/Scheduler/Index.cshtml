@model IList<Interview_Prep.Controllers.Meeting>

@{
    ViewBag.Title = "Index";
}

@for(int i = 0; i < Model.Count; i++)
{
    <h2>
        @Model[i].Name
        <br />
        @Model[i].Length
    </h2>
}